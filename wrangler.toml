name = "signaler-worker"
main = "index.js"
compatibility_date = "2025-11-30"

# Durable Object binding: env.ROOM -> class RoomDO
[durable_objects]
bindings = [
  { name = "ROOM", class_name = "RoomDO" }
]

# Required for Durable Objects on the current free plan:
# create a SQLite-backed namespace for RoomDO.
[[migrations]]
tag = "v1"
new_sqlite_classes = ["RoomDO"]
